<div data-controller="background" class="bg-fit bg-cover px-4 py-5 mx-auto sm:max-w-xl md:max-w-full lg:max-w-screen-2xl md:px-8 md:pb-10 md:pt-5 min-h-screen" style="background-image: url('<%= image_path('challenge_post/challenge.jpeg') %>'); ">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-4 py-12">
    <div class="w-full text-center pb-8">
      <h1 class="font-bold text-3xl md:text-4xl lg:text-5xl font-heading text-gray-900 pb-2">
        CHALLENGE
      </h1>
      <p class="text-blue-600 font-normal text-base">こんなチャレンジをしてみませんか？</p>
    </div>
    <div class="mb-10">
      <%= render 'search_form' %>
    </div>
    <div class="w-full grid grid-cols-1 md:grid-cols-2 gap-6">
      <% @challenge_posts.each do |challenge_post| %>
        <%= render 'post', { challenge_post: challenge_post } %>
      <% end %>
    </div>
  </div>
  <div class="flex justify-center pt-10">
    <%= paginate @challenge_posts %>
  </div>
</div>