<%= form_with model: @post, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :body %>
  <%= f.text_field :body %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>