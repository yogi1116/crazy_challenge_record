<%= turbo_stream.replace "unlike-#{@post.id}" do %>
  <%= render 'posts/like', post: @post %>
<% end %>

<%= turbo_stream.replace "likes_count-#{@post.id}" do %>
  <%= render 'posts/likes_count', post: @post %>
<% end %>